{
  "_comment": "Preferred: uvx — no pip install needed, UV manages the isolated env automatically. Add --devtools/--devtools-project in args if you want the optional DevTools sidecar.",
  "mcpServers": {
    "graphbase-memories": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "--from", "/home/<you>/Workspace/my-templates/graphbase-memories-mcp",
        "graphbase-memories-mcp"
      ],
      "env": {
        "GRAPHBASE_PROJECT":   "your-project-slug",
        "GRAPHBASE_DATA_DIR":  "~/.graphbase",
        "GRAPHBASE_BACKEND":   "sqlite",
        "GRAPHBASE_LOG_LEVEL": "WARNING"
      }
    }
  }
}
